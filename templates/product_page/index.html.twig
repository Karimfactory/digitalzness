{% extends 'base.html.twig' %}
 
{% block title %}{{ product.name }}{% endblock %}
 
{% block body %}

	<div>
		<h2>{{ product.name }}</h2>
		<img src="{{ asset(product.picture) }}" width="150px">
		<p>
			{{ product.price }}€
			<button id="add_cart" data-url="{{ url('add_cart', {  slug : product.slug }) }}" type="button" class="btn btn-primary">Ajouter au panier</button>
		</p>
	</div>

	<pre>
		{{ dump(product) }}
	</pre>
	


{% endblock %}


{% block javascripts %}
<script src="{{ asset('assets/javascript/cart.js') }}"></script>
{% endblock %}
